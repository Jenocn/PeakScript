
#include "VirtualMachine.h"

using namespace peak;

std::shared_ptr<VirtualJourney> VirtualMachine::Load(const std::string& src) {
	auto journey = std::shared_ptr<VirtualJourney>(new VirtualJourney());
	// temp todo...
	return journey;
}
